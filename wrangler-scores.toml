name = "pfpi-scores-worker"
main = "worker.js"
compatibility_date = "2026-08-24"

[[kv_namespaces]]
binding = "PFPI_KV"
id = "3b5cd856fa7b40908601404f46b95456"

# Cron trigger (live-scores polling) is configured in the Cloudflare
# dashboard Triggers tab, not here. See BUILD_LOG.md for the recommended
# cron string. Kept out of wrangler.toml so deploys never silently overwrite
# whatever cadence is actually live in the dashboard.
