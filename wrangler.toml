name = "pfpi-picks-worker"
main = "picks-worker.js"
compatibility_date = "2026-08-24"

[[kv_namespaces]]
binding = "PFPI_KV"
id = "3b5cd856fa7b40908601404f46b95456"

# Cron trigger (Tuesday 7am ET weekly picks email, guarded internally so the
# hourly firing only actually sends once it's genuinely Tuesday 7am
# America/New_York) is configured in the Cloudflare dashboard Triggers tab,
# not here. See BUILD_LOG.md for the exact recommended cron string and why
# it's kept out of wrangler.toml (lesson learned from Cote Cup: config here
# can drift from what's actually live in the dashboard and mislead).
